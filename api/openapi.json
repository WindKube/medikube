{
  "components": {
    "schemas": {
      "ErrorEnvelope": {
        "additionalProperties": false,
        "properties": {
          "error": {
            "additionalProperties": false,
            "properties": {
              "code": {
                "type": "string"
              },
              "fields": {
                "items": {
                  "additionalProperties": false,
                  "properties": {
                    "code": {
                      "type": "string"
                    },
                    "field": {
                      "type": "string"
                    },
                    "message": {
                      "type": "string"
                    }
                  },
                  "required": [
                    "code",
                    "field",
                    "message"
                  ],
                  "type": "object"
                },
                "type": "array"
              },
              "message": {
                "type": "string"
              },
              "request_id": {
                "type": "string"
              }
            },
            "required": [
              "code",
              "message",
              "request_id"
            ],
            "type": "object"
          }
        },
        "required": [
          "error"
        ],
        "type": "object"
      },
      "Record": {
        "discriminator": {
          "mapping": {
            "medication": "#/components/schemas/Record_medications"
          },
          "propertyName": "kind"
        },
        "oneOf": [
          {
            "$ref": "#/components/schemas/Record_medications"
          }
        ]
      },
      "RecordCreate": {
        "oneOf": [
          {
            "$ref": "#/components/schemas/RecordCreate_medications"
          }
        ]
      },
      "RecordCreate_medications": {
        "additionalProperties": false,
        "properties": {
          "alternative_name": {
            "type": "string"
          },
          "dosage": {
            "type": "string"
          },
          "ended_on": {
            "type": "string"
          },
          "frequency": {
            "type": "string"
          },
          "indication": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "notes": {
            "type": "string"
          },
          "route": {
            "type": "string"
          },
          "side_effects": {
            "type": "string"
          },
          "started_on": {
            "type": "string"
          },
          "status": {
            "type": "string"
          },
          "type": {
            "type": "string"
          }
        },
        "required": [
          "name"
        ],
        "type": "object"
      },
      "RecordPatch": {
        "oneOf": [
          {
            "$ref": "#/components/schemas/RecordPatch_medications"
          }
        ]
      },
      "RecordPatch_medications": {
        "additionalProperties": false,
        "properties": {
          "alternative_name": {
            "type": "string"
          },
          "dosage": {
            "type": "string"
          },
          "ended_on": {
            "type": [
              "string",
              "null"
            ]
          },
          "frequency": {
            "type": "string"
          },
          "indication": {
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "notes": {
            "type": "string"
          },
          "route": {
            "type": "string"
          },
          "side_effects": {
            "type": "string"
          },
          "started_on": {
            "type": [
              "string",
              "null"
            ]
          },
          "status": {
            "type": "string"
          },
          "type": {
            "type": "string"
          }
        },
        "type": "object"
      },
      "RecordSummary": {
        "discriminator": {
          "mapping": {
            "medication": "#/components/schemas/RecordSummary_medications"
          },
          "propertyName": "kind"
        },
        "oneOf": [
          {
            "$ref": "#/components/schemas/RecordSummary_medications"
          }
        ]
      },
      "RecordSummaryPage": {
        "additionalProperties": false,
        "properties": {
          "items": {
            "items": {
              "$ref": "#/components/schemas/RecordSummary"
            },
            "type": "array"
          },
          "next_cursor": {
            "type": [
              "string",
              "null"
            ]
          },
          "total": {
            "type": "integer"
          }
        },
        "required": [
          "items",
          "next_cursor"
        ],
        "type": "object"
      },
      "RecordSummary_medications": {
        "additionalProperties": false,
        "properties": {
          "dosage": {
            "type": "string"
          },
          "frequency": {
            "type": "string"
          },
          "id": {
            "type": "string"
          },
          "kind": {
            "enum": [
              "medication"
            ],
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "started_on": {
            "type": [
              "string",
              "null"
            ]
          },
          "status": {
            "type": "string"
          },
          "updated_at": {
            "type": "string"
          }
        },
        "required": [
          "id",
          "kind",
          "name",
          "started_on",
          "status",
          "updated_at"
        ],
        "type": "object"
      },
      "Record_medications": {
        "additionalProperties": false,
        "properties": {
          "alternative_name": {
            "type": "string"
          },
          "created_at": {
            "type": "string"
          },
          "dosage": {
            "type": "string"
          },
          "ended_on": {
            "type": [
              "string",
              "null"
            ]
          },
          "frequency": {
            "type": "string"
          },
          "id": {
            "type": "string"
          },
          "indication": {
            "type": "string"
          },
          "kind": {
            "enum": [
              "medication"
            ],
            "type": "string"
          },
          "name": {
            "type": "string"
          },
          "notes": {
            "type": "string"
          },
          "route": {
            "type": "string"
          },
          "side_effects": {
            "type": "string"
          },
          "started_on": {
            "type": [
              "string",
              "null"
            ]
          },
          "status": {
            "type": "string"
          },
          "type": {
            "type": "string"
          },
          "updated_at": {
            "type": "string"
          }
        },
        "required": [
          "created_at",
          "ended_on",
          "id",
          "kind",
          "name",
          "started_on",
          "status",
          "updated_at"
        ],
        "type": "object"
      }
    },
    "securitySchemes": {
      "session": {
        "description": "The session token, in the Authorization header. A browser carries the same token in an HttpOnly, SameSite cookie that the server turns back into this header before anything else in the stack sees the request, so both callers behave identically from here on.",
        "scheme": "bearer",
        "type": "http"
      }
    }
  },
  "info": {
    "description": "The published interface of a MediKube instance, generated from the route registry rather than written by hand. Every non-2xx response carries the same error envelope, and every response carries the request id that appears on the log lines for the same request.",
    "title": "MediKube API",
    "version": "0.1.0"
  },
  "openapi": "3.1.0",
  "paths": {
    "/_/{path}": {
      "get": {
        "description": "Authorization: requires a PocketBase superuser session.\n\nServed by PocketBase, not by MediKube. It is documented so that a reachable path is never one nobody wrote down, and its failures carry PocketBase's error shape rather than MediKube's. It ships in production, hardened: mandatory superuser MFA, mandatory IP allowlist, every session audited.",
        "operationId": "nativeAdminUI",
        "parameters": [
          {
            "description": "Everything below the prefix. PocketBase serves it.",
            "in": "path",
            "name": "path",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "PocketBase's own response."
          }
        },
        "security": [
          {
            "session": []
          }
        ],
        "summary": "The PocketBase superuser admin UI. It ships in production, hardened: mandatory MFA, mandatory IP allowlist, every session audited (constitution VII)."
      }
    },
    "/api/collections/_superusers/auth-methods": {
      "get": {
        "description": "Authorization: requires a PocketBase superuser session.\n\nServed by PocketBase, not by MediKube. It is documented so that a reachable path is never one nobody wrote down, and its failures carry PocketBase's error shape rather than MediKube's. The admin UI's own auth-method discovery.",
        "operationId": "nativeSuperuserAuthMethods",
        "responses": {
          "200": {
            "description": "PocketBase's own response."
          }
        },
        "security": [
          {
            "session": []
          }
        ],
        "summary": "The admin UI's own auth-method discovery."
      }
    },
    "/api/collections/_superusers/auth-refresh": {
      "post": {
        "description": "Authorization: requires a PocketBase superuser session.\n\nServed by PocketBase, not by MediKube. It is documented so that a reachable path is never one nobody wrote down, and its failures carry PocketBase's error shape rather than MediKube's. The admin UI's own session refresh.",
        "operationId": "nativeSuperuserAuthRefresh",
        "responses": {
          "200": {
            "description": "PocketBase's own response."
          }
        },
        "security": [
          {
            "session": []
          }
        ],
        "summary": "The admin UI's own session refresh."
      }
    },
    "/api/collections/_superusers/auth-with-password": {
      "post": {
        "description": "Authorization: requires a PocketBase superuser session.\n\nServed by PocketBase, not by MediKube. It is documented so that a reachable path is never one nobody wrote down, and its failures carry PocketBase's error shape rather than MediKube's. The admin UI's own authentication.",
        "operationId": "nativeSuperuserAuthWithPassword",
        "responses": {
          "200": {
            "description": "PocketBase's own response."
          }
        },
        "security": [
          {
            "session": []
          }
        ],
        "summary": "The admin UI's own authentication."
      }
    },
    "/api/collections/users/auth-methods": {
      "get": {
        "description": "Authorization: public. No session is required, and the response names no account.\n\nServed by PocketBase, not by MediKube. It is documented so that a reachable path is never one nobody wrote down, and its failures carry PocketBase's error shape rather than MediKube's. PocketBase-native auth-method discovery.",
        "operationId": "nativeUserAuthMethods",
        "responses": {
          "200": {
            "description": "PocketBase's own response."
          }
        },
        "security": [],
        "summary": "PocketBase-native auth-method discovery."
      }
    },
    "/api/collections/users/auth-refresh": {
      "post": {
        "description": "Authorization: requires a session.\n\nServed by PocketBase, not by MediKube. It is documented so that a reachable path is never one nobody wrote down, and its failures carry PocketBase's error shape rather than MediKube's. PocketBase-native session refresh.",
        "operationId": "nativeUserAuthRefresh",
        "responses": {
          "200": {
            "description": "PocketBase's own response."
          }
        },
        "security": [
          {
            "session": []
          }
        ],
        "summary": "PocketBase-native session refresh."
      }
    },
    "/api/collections/users/auth-with-password": {
      "post": {
        "description": "Authorization: public. No session is required, and the response names no account.\n\nServed by PocketBase, not by MediKube. It is documented so that a reachable path is never one nobody wrote down, and its failures carry PocketBase's error shape rather than MediKube's. The lockdown is scoped to the record subtree precisely so this survives. MediKube's own sign-in is the supported path, and both are audited from the same hook.",
        "operationId": "nativeUserAuthWithPassword",
        "responses": {
          "200": {
            "description": "PocketBase's own response."
          }
        },
        "security": [],
        "summary": "PocketBase-native sign-in. MediKube's /api/v1/auth/login is the supported path; both are audited, because the row is written from OnRecordAuthRequest rather than from a handler (research D-14)."
      }
    },
    "/api/collections/users/confirm-password-reset": {
      "post": {
        "description": "Authorization: public. No session is required, and the response names no account.\n\nServed by PocketBase, not by MediKube. It is documented so that a reachable path is never one nobody wrote down, and its failures carry PocketBase's error shape rather than MediKube's. PocketBase-native recovery confirmation.",
        "operationId": "nativeUserConfirmPasswordReset",
        "responses": {
          "200": {
            "description": "PocketBase's own response."
          }
        },
        "security": [],
        "summary": "PocketBase-native recovery confirmation."
      }
    },
    "/api/collections/users/confirm-verification": {
      "post": {
        "description": "Authorization: public. No session is required, and the response names no account.\n\nServed by PocketBase, not by MediKube. It is documented so that a reachable path is never one nobody wrote down, and its failures carry PocketBase's error shape rather than MediKube's. PocketBase-native confirmation.",
        "operationId": "nativeUserConfirmVerification",
        "responses": {
          "200": {
            "description": "PocketBase's own response."
          }
        },
        "security": [],
        "summary": "PocketBase-native confirmation."
      }
    },
    "/api/collections/users/request-password-reset": {
      "post": {
        "description": "Authorization: public. No session is required, and the response names no account.\n\nServed by PocketBase, not by MediKube. It is documented so that a reachable path is never one nobody wrote down, and its failures carry PocketBase's error shape rather than MediKube's. It enforces the same token rules as MediKube's own recovery path, because it is the same code that path calls.",
        "operationId": "nativeUserRequestPasswordReset",
        "responses": {
          "200": {
            "description": "PocketBase's own response."
          }
        },
        "security": [],
        "summary": "PocketBase-native recovery request. Same token rules as MediKube's, because it is the same code MediKube's handler calls."
      }
    },
    "/api/collections/users/request-verification": {
      "post": {
        "description": "Authorization: public. No session is required, and the response names no account.\n\nServed by PocketBase, not by MediKube. It is documented so that a reachable path is never one nobody wrote down, and its failures carry PocketBase's error shape rather than MediKube's. PocketBase-native confirmation request.",
        "operationId": "nativeUserRequestVerification",
        "responses": {
          "200": {
            "description": "PocketBase's own response."
          }
        },
        "security": [],
        "summary": "PocketBase-native confirmation request."
      }
    },
    "/api/v1/auth/config": {
      "get": {
        "description": "Authorization: public. No session is required, and the response names no account.\n\nIt reveals nothing about any account: not how many exist, not whether a given address is registered.",
        "operationId": "getAuthConfig",
        "responses": {
          "200": {
            "description": "Whether registration is open and the password rules to publish."
          },
          "500": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`internal_error`. The message is a constant: no internal error string ever reaches a client."
          }
        },
        "security": [],
        "summary": "What this instance allows: whether registration is open and whether it can send mail."
      }
    },
    "/api/v1/auth/login": {
      "post": {
        "description": "Authorization: public. No session is required, and the response names no account.\n\nAn unknown address, a wrong password and a disabled account produce the identical 401, byte-identical apart from `request_id`, so none of the three can be probed (FR-005).",
        "operationId": "login",
        "responses": {
          "200": {
            "description": "A session."
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`unauthenticated`. The body names no account and no resource."
          },
          "429": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`rate_limited`."
          },
          "500": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`internal_error`. The message is a constant: no internal error string ever reaches a client."
          }
        },
        "security": [],
        "summary": "Exchange an address and a password for a session."
      }
    },
    "/api/v1/auth/logout": {
      "post": {
        "description": "Authorization: requires a session.\n\nEvery session the account still had open ends too, which is what FR-007 asks for.",
        "operationId": "logout",
        "responses": {
          "204": {
            "description": "The session ended."
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`unauthenticated`. The body names no account and no resource."
          },
          "500": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`internal_error`. The message is a constant: no internal error string ever reaches a client."
          }
        },
        "security": [
          {
            "session": []
          }
        ],
        "summary": "End the current session."
      }
    },
    "/api/v1/auth/password-reset": {
      "post": {
        "description": "Authorization: public. No session is required, and the response names no account.\n\nA recovery form that answers \"no such account\" is an account-existence oracle, so all three outcomes answer identically apart from `request_id` (FR-073).",
        "operationId": "requestPasswordReset",
        "responses": {
          "202": {
            "description": "Accepted. The body is the same whether or not the address has an account."
          },
          "429": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`rate_limited`."
          },
          "500": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`internal_error`. The message is a constant: no internal error string ever reaches a client."
          },
          "503": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`mail_unconfigured`: this instance has no outgoing mail configured, so the request is refused rather than accepted as though it had succeeded (FR-076)."
          }
        },
        "security": [],
        "summary": "Send a recovery link. Answers identically whether or not the address has an account (FR-073)."
      }
    },
    "/api/v1/auth/password-reset/confirm": {
      "post": {
        "description": "Authorization: public. No session is required, and the response names no account.\n\nExpired, already used and tampered-with tokens share one refusal; distinguishing them would tell a caller which tokens once existed.",
        "operationId": "confirmPasswordReset",
        "responses": {
          "204": {
            "description": "The password was set and every session issued before the change stopped working."
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`invalid_token` for a recovery or confirmation token that is expired, already used or tampered with — one code for all three, so no token's former existence is disclosed — or `invalid_cursor` for a forged, tampered or unparseable cursor."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`validation_failed`, with every offending member reported at once in `fields[]`. An unknown or unexpected member is reported with the field code `unknown_field`."
          },
          "429": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`rate_limited`."
          },
          "500": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`internal_error`. The message is a constant: no internal error string ever reaches a client."
          }
        },
        "security": [],
        "summary": "Set a new password from a recovery token."
      }
    },
    "/api/v1/auth/refresh": {
      "post": {
        "description": "Authorization: requires a session.\n\nA token invalidated by a password change or a sign-out elsewhere is refused here like any other.",
        "operationId": "refreshSession",
        "responses": {
          "200": {
            "description": "A fresh session."
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`unauthenticated`. The body names no account and no resource."
          },
          "500": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`internal_error`. The message is a constant: no internal error string ever reaches a client."
          }
        },
        "security": [
          {
            "session": []
          }
        ],
        "summary": "Extend the current session."
      }
    },
    "/api/v1/auth/register": {
      "post": {
        "description": "Authorization: public. No session is required, and the response names no account.\n\nThe conflict message names no address: confirming that a specific person has an account here is itself a disclosure.",
        "operationId": "register",
        "responses": {
          "201": {
            "description": "The account was created and signed in, in one transaction.",
            "headers": {
              "Location": {
                "description": "The address of the created resource.",
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`registration_closed`, or `forbidden` where the resource's existence is already known to the caller. Never returned for a clinical record."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`conflict`. The message names no address and no record."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`validation_failed`, with every offending member reported at once in `fields[]`. An unknown or unexpected member is reported with the field code `unknown_field`."
          },
          "429": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`rate_limited`."
          },
          "500": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`internal_error`. The message is a constant: no internal error string ever reaches a client."
          }
        },
        "security": [],
        "summary": "Create an account. 403 registration_closed when registration is disabled, which is the default."
      }
    },
    "/api/v1/auth/verify-email": {
      "post": {
        "description": "Authorization: requires a session.\n\nIt takes no body: the address is read from the authenticated record, because accepting one would let a caller aim this instance's mailer at a stranger.",
        "operationId": "requestEmailVerification",
        "responses": {
          "202": {
            "description": "Accepted. An address that is already confirmed answers the same way."
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`unauthenticated`. The body names no account and no resource."
          },
          "429": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`rate_limited`."
          },
          "500": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`internal_error`. The message is a constant: no internal error string ever reaches a client."
          },
          "503": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`mail_unconfigured`: this instance has no outgoing mail configured, so the request is refused rather than accepted as though it had succeeded (FR-076)."
          }
        },
        "security": [
          {
            "session": []
          }
        ],
        "summary": "Resend the confirmation to the signed-in account's own address."
      }
    },
    "/api/v1/auth/verify-email/confirm": {
      "post": {
        "description": "Authorization: public. No session is required, and the response names no account.\n\nPublic, because the person following the link may not be signed in on that device.",
        "operationId": "confirmEmailVerification",
        "responses": {
          "204": {
            "description": "The address is confirmed."
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`invalid_token` for a recovery or confirmation token that is expired, already used or tampered with — one code for all three, so no token's former existence is disclosed — or `invalid_cursor` for a forged, tampered or unparseable cursor."
          },
          "429": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`rate_limited`."
          },
          "500": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`internal_error`. The message is a constant: no internal error string ever reaches a client."
          }
        },
        "security": [],
        "summary": "Confirm an address from a confirmation token."
      }
    },
    "/api/v1/facilities": {
      "get": {
        "description": "Authorization: requires a session. Owner-scoped: the owner is taken from the stored record or from the authenticated actor and never from a caller-supplied parameter, and anything the signed-in account does not own answers 404 — byte-identical apart from `request_id` to an id that never existed (FR-032, FR-033).\n\nAlso the kind filter (FR-036) and the type-ahead behind every facility and pharmacy picker (FR-039): one operation, not six.",
        "operationId": "listFacilities",
        "parameters": [
          {
            "description": "Case-insensitive substring match over name and brand.",
            "in": "query",
            "name": "q",
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "One of practice, pharmacy, hospital, lab, imaging, other. Outside the vocabulary, 422.",
            "in": "query",
            "name": "kind",
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "Page size, 1..100.",
            "in": "query",
            "name": "limit",
            "schema": {
              "default": 25,
              "maximum": 100,
              "minimum": 1,
              "type": "integer"
            }
          },
          {
            "description": "An opaque, server-minted continuation token encoding the sort keys and the last id — never an offset, so a row inserted or deleted between two pages cannot shift a boundary. A forged or unparseable cursor is 400.",
            "in": "query",
            "name": "cursor",
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "Include `total` in the envelope. Absent means the count is not computed.",
            "in": "query",
            "name": "count",
            "schema": {
              "type": "boolean"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "A page of the account's directory of places of care."
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`invalid_token` for a recovery or confirmation token that is expired, already used or tampered with — one code for all three, so no token's former existence is disclosed — or `invalid_cursor` for a forged, tampered or unparseable cursor."
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`unauthenticated`. The body names no account and no resource."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`validation_failed`, with every offending member reported at once in `fields[]`. An unknown or unexpected member is reported with the field code `unknown_field`."
          },
          "500": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`internal_error`. The message is a constant: no internal error string ever reaches a client."
          }
        },
        "security": [
          {
            "session": []
          }
        ],
        "summary": "The account's directory of places of care, cursor-paginated. Also the type-ahead behind every facility picker."
      },
      "post": {
        "description": "Authorization: requires a session. Owner-scoped: the owner is taken from the stored record or from the authenticated actor and never from a caller-supplied parameter, and anything the signed-in account does not own answers 404 — byte-identical apart from `request_id` to an id that never existed (FR-032, FR-033).\n\nThere is deliberately no uniqueness constraint on name: a chain's second branch is a second entry with its own address and hours (FR-035, US5-3).",
        "operationId": "createFacility",
        "responses": {
          "201": {
            "description": "The created facility.",
            "headers": {
              "ETag": {
                "description": "The record's current version, derived from `updated`. It is what `If-Match` is compared against.",
                "schema": {
                  "type": "string"
                }
              },
              "Location": {
                "description": "The address of the created resource.",
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`unauthenticated`. The body names no account and no resource."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`validation_failed`, with every offending member reported at once in `fields[]`. An unknown or unexpected member is reported with the field code `unknown_field`."
          },
          "500": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`internal_error`. The message is a constant: no internal error string ever reaches a client."
          }
        },
        "security": [
          {
            "session": []
          }
        ],
        "summary": "Add a facility to the account's directory."
      }
    },
    "/api/v1/facilities/{id}": {
      "delete": {
        "description": "Authorization: requires a session. Owner-scoped: the owner is taken from the stored record or from the authenticated actor and never from a caller-supplied parameter, and anything the signed-in account does not own answers 404 — byte-identical apart from `request_id` to an id that never existed (FR-032, FR-033).\n\nReferences from a practitioner's facility and a medication's pharmacy are unset, not cascaded (research D-06).",
        "operationId": "deleteFacility",
        "parameters": [
          {
            "description": "The record's id.",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "The version the caller last saw, from the record's `ETag`. Required: an optional precondition is a precondition nobody sends. A mismatch is 412.",
            "in": "header",
            "name": "If-Match",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "The facility is permanently gone."
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`unauthenticated`. The body names no account and no resource."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`not_found`. On owner-scoped data this is also the answer for a record belonging to somebody else, byte-identical apart from `request_id` to an id that never existed (FR-033)."
          },
          "412": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`version_mismatch`: the `If-Match` precondition did not match the stored version."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`validation_failed`, with every offending member reported at once in `fields[]`. An unknown or unexpected member is reported with the field code `unknown_field`."
          },
          "500": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`internal_error`. The message is a constant: no internal error string ever reaches a client."
          }
        },
        "security": [
          {
            "session": []
          }
        ],
        "summary": "Delete a facility. If-Match is required. Every referencing record survives with the reference cleared."
      },
      "get": {
        "description": "Authorization: requires a session. Owner-scoped: the owner is taken from the stored record or from the authenticated actor and never from a caller-supplied parameter, and anything the signed-in account does not own answers 404 — byte-identical apart from `request_id` to an id that never existed (FR-032, FR-033).",
        "operationId": "getFacility",
        "parameters": [
          {
            "description": "The record's id.",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The facility, including `usage`: the practitioners that name it and the clinical records whose pharmacy it is.",
            "headers": {
              "ETag": {
                "description": "The record's current version, derived from `updated`. It is what `If-Match` is compared against.",
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`unauthenticated`. The body names no account and no resource."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`not_found`. On owner-scoped data this is also the answer for a record belonging to somebody else, byte-identical apart from `request_id` to an id that never existed (FR-033)."
          },
          "500": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`internal_error`. The message is a constant: no internal error string ever reaches a client."
          }
        },
        "security": [
          {
            "session": []
          }
        ],
        "summary": "One facility, with usage. Another account's id answers 404."
      },
      "patch": {
        "description": "Authorization: requires a session. Owner-scoped: the owner is taken from the stored record or from the authenticated actor and never from a caller-supplied parameter, and anything the signed-in account does not own answers 404 — byte-identical apart from `request_id` to an id that never existed (FR-032, FR-033).\n\nOnly supplied members change. Changing `kind` is permitted: a practice that turns out to be a hospital is a correction, not a new entity.",
        "operationId": "updateFacility",
        "parameters": [
          {
            "description": "The record's id.",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "The version the caller last saw, from the record's `ETag`. Required: an optional precondition is a precondition nobody sends. A mismatch is 412.",
            "in": "header",
            "name": "If-Match",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The updated facility.",
            "headers": {
              "ETag": {
                "description": "The record's current version, derived from `updated`. It is what `If-Match` is compared against.",
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`unauthenticated`. The body names no account and no resource."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`not_found`. On owner-scoped data this is also the answer for a record belonging to somebody else, byte-identical apart from `request_id` to an id that never existed (FR-033)."
          },
          "412": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`version_mismatch`: the `If-Match` precondition did not match the stored version."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`validation_failed`, with every offending member reported at once in `fields[]`. An unknown or unexpected member is reported with the field code `unknown_field`."
          },
          "500": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`internal_error`. The message is a constant: no internal error string ever reaches a client."
          }
        },
        "security": [
          {
            "session": []
          }
        ],
        "summary": "Partial update. If-Match is required; a mismatch is 412 carrying the current representation."
      }
    },
    "/api/v1/healthz": {
      "get": {
        "description": "Authorization: public. No session is required, and the response names no account.\n\nIt touches no database and no filesystem, and its only other outcome is not answering at all (FR-052).",
        "operationId": "healthz",
        "responses": {
          "200": {
            "description": "The process is running."
          },
          "500": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`internal_error`. The message is a constant: no internal error string ever reaches a client."
          }
        },
        "security": [],
        "summary": "Liveness. Answers as long as the process is running."
      }
    },
    "/api/v1/me": {
      "delete": {
        "description": "Authorization: requires a session.\n\nIt requires the current password and a typed confirmation phrase, and it cannot be undone.",
        "operationId": "deleteMe",
        "responses": {
          "204": {
            "description": "The account and everything it owns are permanently gone."
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`unauthenticated`. The body names no account and no resource."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`validation_failed`, with every offending member reported at once in `fields[]`. An unknown or unexpected member is reported with the field code `unknown_field`."
          },
          "500": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`internal_error`. The message is a constant: no internal error string ever reaches a client."
          }
        },
        "security": [
          {
            "session": []
          }
        ],
        "summary": "Delete the account and everything it owns, in one transaction. There is no soft delete."
      },
      "get": {
        "description": "Authorization: requires a session.\n\nThere is no id to supply and no other account this can reach.",
        "operationId": "getMe",
        "responses": {
          "200": {
            "description": "The signed-in account."
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`unauthenticated`. The body names no account and no resource."
          },
          "500": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`internal_error`. The message is a constant: no internal error string ever reaches a client."
          }
        },
        "security": [
          {
            "session": []
          }
        ],
        "summary": "The signed-in account: address, display name and preferences."
      },
      "patch": {
        "description": "Authorization: requires a session.\n\nThe address, the role and the disabled state are not members of the request body, so they cannot be changed here at all (FR-012).",
        "operationId": "updateMe",
        "responses": {
          "200": {
            "description": "The updated account. The response is also the feedback the settings page renders."
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`unauthenticated`. The body names no account and no resource."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`validation_failed`, with every offending member reported at once in `fields[]`. An unknown or unexpected member is reported with the field code `unknown_field`."
          },
          "500": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`internal_error`. The message is a constant: no internal error string ever reaches a client."
          }
        },
        "security": [
          {
            "session": []
          }
        ],
        "summary": "Change the display name or the preferences."
      }
    },
    "/api/v1/me/password": {
      "put": {
        "description": "Authorization: requires a session.\n\nA wrong current password is 422 on that member rather than 401: the caller is authenticated, the password is what failed.",
        "operationId": "changePassword",
        "responses": {
          "204": {
            "description": "The password changed and every other session stopped working."
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`unauthenticated`. The body names no account and no resource."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`validation_failed`, with every offending member reported at once in `fields[]`. An unknown or unexpected member is reported with the field code `unknown_field`."
          },
          "500": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`internal_error`. The message is a constant: no internal error string ever reaches a client."
          }
        },
        "security": [
          {
            "session": []
          }
        ],
        "summary": "Replace the password and rotate the token key, which ends every other session."
      }
    },
    "/api/v1/practitioners": {
      "get": {
        "description": "Authorization: requires a session. Owner-scoped: the owner is taken from the stored record or from the authenticated actor and never from a caller-supplied parameter, and anything the signed-in account does not own answers 404 — byte-identical apart from `request_id` to an id that never existed (FR-032, FR-033).\n\nAlso the type-ahead behind every practitioner picker: `?q=` with a short prefix and `?limit=10` is the autocomplete call, and there is no separate operation for it.",
        "operationId": "listPractitioners",
        "parameters": [
          {
            "description": "Case-insensitive substring match over name.",
            "in": "query",
            "name": "q",
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "Narrow to one specialty from the fixed vocabulary. Outside it, 422.",
            "in": "query",
            "name": "specialty",
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "Narrow to practitioners at one facility.",
            "in": "query",
            "name": "facility",
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "Page size, 1..100.",
            "in": "query",
            "name": "limit",
            "schema": {
              "default": 25,
              "maximum": 100,
              "minimum": 1,
              "type": "integer"
            }
          },
          {
            "description": "An opaque, server-minted continuation token encoding the sort keys and the last id — never an offset, so a row inserted or deleted between two pages cannot shift a boundary. A forged or unparseable cursor is 400.",
            "in": "query",
            "name": "cursor",
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "Include `total` in the envelope. Absent means the count is not computed.",
            "in": "query",
            "name": "count",
            "schema": {
              "type": "boolean"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "A page of the account's practitioner directory."
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`invalid_token` for a recovery or confirmation token that is expired, already used or tampered with — one code for all three, so no token's former existence is disclosed — or `invalid_cursor` for a forged, tampered or unparseable cursor."
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`unauthenticated`. The body names no account and no resource."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`validation_failed`, with every offending member reported at once in `fields[]`. An unknown or unexpected member is reported with the field code `unknown_field`."
          },
          "500": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`internal_error`. The message is a constant: no internal error string ever reaches a client."
          }
        },
        "security": [
          {
            "session": []
          }
        ],
        "summary": "The account's practitioner directory, cursor-paginated. Also the type-ahead behind every practitioner picker."
      },
      "post": {
        "description": "Authorization: requires a session. Owner-scoped: the owner is taken from the stored record or from the authenticated actor and never from a caller-supplied parameter, and anything the signed-in account does not own answers 404 — byte-identical apart from `request_id` to an id that never existed (FR-032, FR-033).\n\nA duplicate `(owner, name, specialty)` is 409, including when neither has a specialty. A `facility` naming a facility the actor does not own is 404 (FR-042).",
        "operationId": "createPractitioner",
        "responses": {
          "201": {
            "description": "The created practitioner.",
            "headers": {
              "ETag": {
                "description": "The record's current version, derived from `updated`. It is what `If-Match` is compared against.",
                "schema": {
                  "type": "string"
                }
              },
              "Location": {
                "description": "The address of the created resource.",
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`unauthenticated`. The body names no account and no resource."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`not_found`. On owner-scoped data this is also the answer for a record belonging to somebody else, byte-identical apart from `request_id` to an id that never existed (FR-033)."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`conflict`. The message names no address and no record."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`validation_failed`, with every offending member reported at once in `fields[]`. An unknown or unexpected member is reported with the field code `unknown_field`."
          },
          "500": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`internal_error`. The message is a constant: no internal error string ever reaches a client."
          }
        },
        "security": [
          {
            "session": []
          }
        ],
        "summary": "Add a practitioner to the account's directory."
      }
    },
    "/api/v1/practitioners/{id}": {
      "delete": {
        "description": "Authorization: requires a session. Owner-scoped: the owner is taken from the stored record or from the authenticated actor and never from a caller-supplied parameter, and anything the signed-in account does not own answers 404 — byte-identical apart from `request_id` to an id that never existed (FR-032, FR-033).\n\nEvery referencing record — a patient's primary practitioner, a medication's practitioner — survives with the reference cleared (research D-06).",
        "operationId": "deletePractitioner",
        "parameters": [
          {
            "description": "The record's id.",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "The version the caller last saw, from the record's `ETag`. Required: an optional precondition is a precondition nobody sends. A mismatch is 412.",
            "in": "header",
            "name": "If-Match",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "The practitioner is permanently gone."
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`unauthenticated`. The body names no account and no resource."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`not_found`. On owner-scoped data this is also the answer for a record belonging to somebody else, byte-identical apart from `request_id` to an id that never existed (FR-033)."
          },
          "412": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`version_mismatch`: the `If-Match` precondition did not match the stored version."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`validation_failed`, with every offending member reported at once in `fields[]`. An unknown or unexpected member is reported with the field code `unknown_field`."
          },
          "500": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`internal_error`. The message is a constant: no internal error string ever reaches a client."
          }
        },
        "security": [
          {
            "session": []
          }
        ],
        "summary": "Delete a practitioner. If-Match is required. Every referencing record survives with the reference cleared."
      },
      "get": {
        "description": "Authorization: requires a session. Owner-scoped: the owner is taken from the stored record or from the authenticated actor and never from a caller-supplied parameter, and anything the signed-in account does not own answers 404 — byte-identical apart from `request_id` to an id that never existed (FR-032, FR-033).",
        "operationId": "getPractitioner",
        "parameters": [
          {
            "description": "The record's id.",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The practitioner, including `usage`: how many patients name them as primary and how many clinical records reference them (FR-040).",
            "headers": {
              "ETag": {
                "description": "The record's current version, derived from `updated`. It is what `If-Match` is compared against.",
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`unauthenticated`. The body names no account and no resource."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`not_found`. On owner-scoped data this is also the answer for a record belonging to somebody else, byte-identical apart from `request_id` to an id that never existed (FR-033)."
          },
          "500": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`internal_error`. The message is a constant: no internal error string ever reaches a client."
          }
        },
        "security": [
          {
            "session": []
          }
        ],
        "summary": "One practitioner, with usage. Another account's id answers 404."
      },
      "patch": {
        "description": "Authorization: requires a session. Owner-scoped: the owner is taken from the stored record or from the authenticated actor and never from a caller-supplied parameter, and anything the signed-in account does not own answers 404 — byte-identical apart from `request_id` to an id that never existed (FR-032, FR-033).\n\nOnly supplied members change. `facility` accepts an explicit null to clear the reference. A missing `If-Match` is 422 on that header; a mismatch is 412 and the response carries the stored practitioner's current version.",
        "operationId": "updatePractitioner",
        "parameters": [
          {
            "description": "The record's id.",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "The version the caller last saw, from the record's `ETag`. Required: an optional precondition is a precondition nobody sends. A mismatch is 412.",
            "in": "header",
            "name": "If-Match",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "The updated practitioner.",
            "headers": {
              "ETag": {
                "description": "The record's current version, derived from `updated`. It is what `If-Match` is compared against.",
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`unauthenticated`. The body names no account and no resource."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`not_found`. On owner-scoped data this is also the answer for a record belonging to somebody else, byte-identical apart from `request_id` to an id that never existed (FR-033)."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`conflict`. The message names no address and no record."
          },
          "412": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`version_mismatch`: the `If-Match` precondition did not match the stored version."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`validation_failed`, with every offending member reported at once in `fields[]`. An unknown or unexpected member is reported with the field code `unknown_field`."
          },
          "500": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`internal_error`. The message is a constant: no internal error string ever reaches a client."
          }
        },
        "security": [
          {
            "session": []
          }
        ],
        "summary": "Partial update. If-Match is required; a mismatch is 412 carrying the current representation."
      }
    },
    "/api/v1/readyz": {
      "get": {
        "description": "Authorization: public. No session is required, and the response names no account.\n\nIt reports whether the instance can serve and nothing about the data it holds.",
        "operationId": "readyz",
        "responses": {
          "200": {
            "description": "The instance can serve. The check vocabulary is fixed and closed: `database`, `migrations`, `storage`, each `ok` or `error`."
          },
          "500": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`internal_error`. The message is a constant: no internal error string ever reaches a client."
          },
          "503": {
            "description": "Not ready, or draining. The body is the readiness payload rather than an error envelope, and it has no message member at all: a driver error can carry a file path, a DSN or a credential, so the reason is a check name and the error itself goes to the log (FR-052)."
          }
        },
        "security": [],
        "summary": "Readiness, including the database. What `medikube healthcheck` dials."
      }
    },
    "/api/v1/records": {
      "get": {
        "description": "Authorization: requires a session. Owner-scoped: the owner is taken from the stored record or from the authenticated actor and never from a caller-supplied parameter, and anything the signed-in account does not own answers 404 — byte-identical apart from `request_id` to an id that never existed (FR-032, FR-033).\n\nThe cross-kind list. Its items are the record union, so a client written against it today keeps working when more kinds are registered.",
        "operationId": "listRecords",
        "parameters": [
          {
            "description": "Restrict to these registered path segments. Absent means every kind. Comma-separated; whitespace is not trimmed.",
            "in": "query",
            "name": "kind",
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "Narrow to records whose primary date is on or after this day.",
            "in": "query",
            "name": "from",
            "schema": {
              "format": "date",
              "type": "string"
            }
          },
          {
            "description": "Narrow to records whose primary date is on or before this day.",
            "in": "query",
            "name": "to",
            "schema": {
              "format": "date",
              "type": "string"
            }
          },
          {
            "description": "Page size, 1..100.",
            "in": "query",
            "name": "limit",
            "schema": {
              "default": 25,
              "maximum": 100,
              "minimum": 1,
              "type": "integer"
            }
          },
          {
            "description": "An opaque, server-minted continuation token encoding the sort keys and the last id — never an offset, so a row inserted or deleted between two pages cannot shift a boundary. A forged or unparseable cursor is 400.",
            "in": "query",
            "name": "cursor",
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "Include `total` in the envelope. Absent means the count is not computed.",
            "in": "query",
            "name": "count",
            "schema": {
              "type": "boolean"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RecordSummaryPage"
                }
              }
            },
            "description": "A page of the signed-in account's records."
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`invalid_token` for a recovery or confirmation token that is expired, already used or tampered with — one code for all three, so no token's former existence is disclosed — or `invalid_cursor` for a forged, tampered or unparseable cursor."
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`unauthenticated`. The body names no account and no resource."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`validation_failed`, with every offending member reported at once in `fields[]`. An unknown or unexpected member is reported with the field code `unknown_field`."
          },
          "500": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`internal_error`. The message is a constant: no internal error string ever reaches a client."
          }
        },
        "security": [
          {
            "session": []
          }
        ],
        "summary": "Every record the signed-in account owns, across kinds, cursor-paginated."
      }
    },
    "/api/v1/records/{kind}": {
      "get": {
        "description": "Authorization: requires a session. Owner-scoped: the owner is taken from the stored record or from the authenticated actor and never from a caller-supplied parameter, and anything the signed-in account does not own answers 404 — byte-identical apart from `request_id` to an id that never existed (FR-032, FR-033).\n\nA {kind} outside the enum is 404 and not 400: an unregistered kind is indistinguishable from a path that does not exist.",
        "operationId": "listRecordsOfKind",
        "parameters": [
          {
            "description": "A registered record kind, spelled as its path segment. A value outside the enum is 404 and not 400: an unregistered kind is indistinguishable from a path that does not exist.",
            "in": "path",
            "name": "kind",
            "required": true,
            "schema": {
              "enum": [
                "medications"
              ],
              "type": "string"
            }
          },
          {
            "description": "Case-insensitive substring match over the kind's searchable text.",
            "in": "query",
            "name": "q",
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "Restrict to these status values. Comma-separated; whitespace is not trimmed.",
            "in": "query",
            "name": "status",
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "Drawn from the kind's allowlist, with a `-` prefix for descending. A value outside the allowlist is 422 and is never silently ignored.",
            "in": "query",
            "name": "sort",
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "Page size, 1..100.",
            "in": "query",
            "name": "limit",
            "schema": {
              "default": 25,
              "maximum": 100,
              "minimum": 1,
              "type": "integer"
            }
          },
          {
            "description": "An opaque, server-minted continuation token encoding the sort keys and the last id — never an offset, so a row inserted or deleted between two pages cannot shift a boundary. A forged or unparseable cursor is 400.",
            "in": "query",
            "name": "cursor",
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "Include `total` in the envelope. Absent means the count is not computed.",
            "in": "query",
            "name": "count",
            "schema": {
              "type": "boolean"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RecordSummaryPage"
                }
              }
            },
            "description": "A page of the signed-in account's records."
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`invalid_token` for a recovery or confirmation token that is expired, already used or tampered with — one code for all three, so no token's former existence is disclosed — or `invalid_cursor` for a forged, tampered or unparseable cursor."
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`unauthenticated`. The body names no account and no resource."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`validation_failed`, with every offending member reported at once in `fields[]`. An unknown or unexpected member is reported with the field code `unknown_field`."
          },
          "500": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`internal_error`. The message is a constant: no internal error string ever reaches a client."
          }
        },
        "security": [
          {
            "session": []
          }
        ],
        "summary": "One kind's records for the signed-in account, cursor-paginated."
      },
      "post": {
        "description": "Authorization: requires a session. Owner-scoped: the owner is taken from the stored record or from the authenticated actor and never from a caller-supplied parameter, and anything the signed-in account does not own answers 404 — byte-identical apart from `request_id` to an id that never existed (FR-032, FR-033).\n\nThe owner is taken from the session. The request body has no owner member at all, so re-attribution is impossible by shape rather than by a runtime check (FR-032).",
        "operationId": "createRecord",
        "parameters": [
          {
            "description": "A registered record kind, spelled as its path segment. A value outside the enum is 404 and not 400: an unregistered kind is indistinguishable from a path that does not exist.",
            "in": "path",
            "name": "kind",
            "required": true,
            "schema": {
              "enum": [
                "medications"
              ],
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RecordCreate"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Record"
                }
              }
            },
            "description": "The created record.",
            "headers": {
              "ETag": {
                "description": "The record's current version, derived from `updated`. It is what `If-Match` is compared against.",
                "schema": {
                  "type": "string"
                }
              },
              "Location": {
                "description": "The address of the created resource.",
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`unauthenticated`. The body names no account and no resource."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`not_found`. On owner-scoped data this is also the answer for a record belonging to somebody else, byte-identical apart from `request_id` to an id that never existed (FR-033)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`validation_failed`, with every offending member reported at once in `fields[]`. An unknown or unexpected member is reported with the field code `unknown_field`."
          },
          "500": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`internal_error`. The message is a constant: no internal error string ever reaches a client."
          }
        },
        "security": [
          {
            "session": []
          }
        ],
        "summary": "Create a record. The owner comes from the session, never from the body."
      }
    },
    "/api/v1/records/{kind}/{id}": {
      "delete": {
        "description": "Authorization: requires a session. Owner-scoped: the owner is taken from the stored record or from the authenticated actor and never from a caller-supplied parameter, and anything the signed-in account does not own answers 404 — byte-identical apart from `request_id` to an id that never existed (FR-032, FR-033).\n\n`If-Match` is required for the same reason as on update: deleting the record you last saw is a different act from deleting whatever is there now.",
        "operationId": "deleteRecord",
        "parameters": [
          {
            "description": "A registered record kind, spelled as its path segment. A value outside the enum is 404 and not 400: an unregistered kind is indistinguishable from a path that does not exist.",
            "in": "path",
            "name": "kind",
            "required": true,
            "schema": {
              "enum": [
                "medications"
              ],
              "type": "string"
            }
          },
          {
            "description": "The record's id.",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "The version the caller last saw, from the record's `ETag`. Required: an optional precondition is a precondition nobody sends. A mismatch is 412.",
            "in": "header",
            "name": "If-Match",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "The record is permanently gone. There is no recycle bin and no undo."
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`unauthenticated`. The body names no account and no resource."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`not_found`. On owner-scoped data this is also the answer for a record belonging to somebody else, byte-identical apart from `request_id` to an id that never existed (FR-033)."
          },
          "412": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`version_mismatch`: the `If-Match` precondition did not match the stored version."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`validation_failed`, with every offending member reported at once in `fields[]`. An unknown or unexpected member is reported with the field code `unknown_field`."
          },
          "500": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`internal_error`. The message is a constant: no internal error string ever reaches a client."
          }
        },
        "security": [
          {
            "session": []
          }
        ],
        "summary": "Delete a record. If-Match is required."
      },
      "get": {
        "description": "Authorization: requires a session. Owner-scoped: the owner is taken from the stored record or from the authenticated actor and never from a caller-supplied parameter, and anything the signed-in account does not own answers 404 — byte-identical apart from `request_id` to an id that never existed (FR-032, FR-033).\n\nMembers that were never filled in are absent from the response rather than present and empty (FR-024).",
        "operationId": "getRecord",
        "parameters": [
          {
            "description": "A registered record kind, spelled as its path segment. A value outside the enum is 404 and not 400: an unregistered kind is indistinguishable from a path that does not exist.",
            "in": "path",
            "name": "kind",
            "required": true,
            "schema": {
              "enum": [
                "medications"
              ],
              "type": "string"
            }
          },
          {
            "description": "The record's id.",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Record"
                }
              }
            },
            "description": "The record.",
            "headers": {
              "ETag": {
                "description": "The record's current version, derived from `updated`. It is what `If-Match` is compared against.",
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`unauthenticated`. The body names no account and no resource."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`not_found`. On owner-scoped data this is also the answer for a record belonging to somebody else, byte-identical apart from `request_id` to an id that never existed (FR-033)."
          },
          "500": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`internal_error`. The message is a constant: no internal error string ever reaches a client."
          }
        },
        "security": [
          {
            "session": []
          }
        ],
        "summary": "One record. Another account's id answers 404, byte-identical to an id that never existed."
      },
      "patch": {
        "description": "Authorization: requires a session. Owner-scoped: the owner is taken from the stored record or from the authenticated actor and never from a caller-supplied parameter, and anything the signed-in account does not own answers 404 — byte-identical apart from `request_id` to an id that never existed (FR-032, FR-033).\n\nOnly supplied members change. A missing `If-Match` is 422 on that header; a mismatch is 412 and the response carries the stored record's current version.",
        "operationId": "updateRecord",
        "parameters": [
          {
            "description": "A registered record kind, spelled as its path segment. A value outside the enum is 404 and not 400: an unregistered kind is indistinguishable from a path that does not exist.",
            "in": "path",
            "name": "kind",
            "required": true,
            "schema": {
              "enum": [
                "medications"
              ],
              "type": "string"
            }
          },
          {
            "description": "The record's id.",
            "in": "path",
            "name": "id",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "description": "The version the caller last saw, from the record's `ETag`. Required: an optional precondition is a precondition nobody sends. A mismatch is 412.",
            "in": "header",
            "name": "If-Match",
            "required": true,
            "schema": {
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RecordPatch"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/Record"
                }
              }
            },
            "description": "The updated record.",
            "headers": {
              "ETag": {
                "description": "The record's current version, derived from `updated`. It is what `If-Match` is compared against.",
                "schema": {
                  "type": "string"
                }
              }
            }
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`unauthenticated`. The body names no account and no resource."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`not_found`. On owner-scoped data this is also the answer for a record belonging to somebody else, byte-identical apart from `request_id` to an id that never existed (FR-033)."
          },
          "412": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`version_mismatch`: the `If-Match` precondition did not match the stored version."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`validation_failed`, with every offending member reported at once in `fields[]`. An unknown or unexpected member is reported with the field code `unknown_field`."
          },
          "500": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`internal_error`. The message is a constant: no internal error string ever reaches a client."
          }
        },
        "security": [
          {
            "session": []
          }
        ],
        "summary": "Partial update. If-Match is required; a mismatch is 412 carrying the current representation."
      }
    },
    "/api/v1/streams/records": {
      "get": {
        "description": "Authorization: requires a session. Owner-scoped: the owner is taken from the stored record or from the authenticated actor and never from a caller-supplied parameter, and anything the signed-in account does not own answers 404 — byte-identical apart from `request_id` to an id that never existed (FR-032, FR-033).\n\nAnonymous is refused before the stream opens: a 401 delivered as an event would be indistinguishable from a working stream that never sends anything. Every event is re-authorised for the subscriber, and the stream carries ids rather than record bodies.",
        "operationId": "streamRecords",
        "parameters": [
          {
            "description": "Restrict to these registered path segments. Absent means every kind. Comma-separated; whitespace is not trimmed.",
            "in": "query",
            "name": "kind",
            "schema": {
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "text/event-stream": {
                "schema": {
                  "type": "string"
                }
              }
            },
            "description": "A Datastar element stream. Two event names are sent and no others: `datastar-patch-elements` for a record that was created, changed or removed, and `datastar-patch-signals` for the heartbeat."
          },
          "401": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`unauthenticated`. The body names no account and no resource."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`validation_failed`, with every offending member reported at once in `fields[]`. An unknown or unexpected member is reported with the field code `unknown_field`."
          },
          "500": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorEnvelope"
                }
              }
            },
            "description": "`internal_error`. The message is a constant: no internal error string ever reaches a client."
          }
        },
        "security": [
          {
            "session": []
          }
        ],
        "summary": "The Datastar element stream. Re-authorised per event; publishes ids, never bodies."
      }
    }
  }
}
