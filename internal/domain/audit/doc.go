// Package audit describes the audit trail's shape and its vocabularies: actor,
// action, target kind, target identifier, timestamp.
//
// There is deliberately no content field. The trail records that something was
// done and by whom, never what the record said.
package audit
