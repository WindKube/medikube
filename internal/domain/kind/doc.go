// Package kind names the clinical record kinds and settles in one place how
// each is spelled as an enum value, as a URL path segment and as a collection.
//
// Deriving all three from one constant is what keeps the API, the router and
// the schema from drifting apart.
package kind
