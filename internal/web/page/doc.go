// Package page holds the handlers behind MediKube's user-facing routes. They
// gather what a view needs and render it; the deciding happens in the services
// they call.
//
// It sits on the PocketBase side of the import boundary.
package page
