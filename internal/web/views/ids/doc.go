// Package ids declares MediKube's DOM identifiers as constants.
//
// Both the templ components and the Go code that patches them read from here,
// so a rename cannot leave a Datastar selector pointing at an element that no
// longer exists.
package ids
