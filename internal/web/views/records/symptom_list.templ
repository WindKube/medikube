package records

import (
	"medikube/internal/domain/kind"
	"medikube/internal/web/views/components"
	"medikube/internal/web/views/ids"
)

const (
	symptomEmptyTitle  = "Nothing recorded yet"
	symptomEmptyBody   = "Record the first episode and it will be listed here, with everything you noted about it."
	symptomCreateLabel = "Record a symptom"
	symptomsTitle      = "Symptoms"
)

templ SymptomList(props SymptomListProps) {
	<section
		id={ ids.RecordList(kind.Symptom) }
		aria-label={ symptomsTitle }
		class="flex flex-col"
	>
		<div class="flex flex-wrap items-center justify-between gap-3">
			<h1
				id={ ids.RecordListHeading(kind.Symptom) }
				tabindex="-1"
				class="text-xl font-semibold text-slate-900 dark:text-slate-100 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-teal-700"
			>{ symptomsTitle }</h1>
			if props.CreateHref != "" {
				<a
					href={ props.CreateHref }
					class="inline-flex items-center rounded-md bg-teal-700 px-3 py-2 text-sm font-medium text-white hover:bg-teal-800 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-teal-700"
				>{ symptomCreateLabel }</a>
			}
		</div>
		<div class="[&:has(tbody_tr)_[data-empty]]:hidden [&:not(:has(tbody_tr))_[data-rows]]:hidden">
			<div data-empty class="mt-6">
				@components.EmptyState(components.EmptyStateProps{
					ID:          ids.RecordEmpty(kind.Symptom),
					Title:       symptomEmptyTitle,
					Body:        symptomEmptyBody,
					ActionHref:  props.CreateHref,
					ActionLabel: symptomCreateLabel,
				})
			</div>
			<div data-rows class="mt-4 overflow-x-auto">
				<table class="w-full border-collapse text-left text-sm">
					<thead>
						<tr class="border-b border-slate-300 text-xs uppercase tracking-wide text-slate-500 dark:border-slate-700 dark:text-slate-400">
							<th scope="col" class="pb-2 pr-4 font-medium">{ SymptomFieldLabel(SymptomFieldName) }</th>
							<th scope="col" class="pb-2 pr-4 font-medium">{ SymptomFieldLabel(SymptomFieldSeverity) }</th>
							<th scope="col" class="pb-2 pr-4 font-medium">Episodes</th>
							<th scope="col" class="pb-2 font-medium">Most recent</th>
						</tr>
					</thead>
					<tbody id={ ids.RecordRows(kind.Symptom) }>
						for _, symptom := range props.Symptoms {
							@SymptomRow(symptom)
						}
					</tbody>
				</table>
			</div>
		</div>
		@components.Pagination(components.PaginationProps{
			ID:           ids.RecordPager(kind.Symptom),
			PreviousHref: props.PreviousHref,
			NextHref:     props.NextHref,
		})
	</section>
}
