// Package api holds the hand-written /api/v1 handlers and the DTOs that mediate
// them. A domain record is never serialised to a client directly.
//
// It sits on the PocketBase side of the import boundary.
package api
