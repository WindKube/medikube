// Package identity decides registration, sign-in and account changes.
//
// It declares the ports it needs — repository, authenticator, auditor, clock —
// and knows nothing about how any of them are implemented.
package identity
