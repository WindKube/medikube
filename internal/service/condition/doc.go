// Package condition decides the condition use cases, authorizing before it
// reads or writes, and adapts itself to the kind-agnostic record service so
// the generic handler can dispatch to it.
package condition
