// Package medicationtest provides the fakes and the shared contract suite for
// the ports package medication declares, so every implementation of a port —
// the fakes included — is held to one contract.
package medicationtest
